<?php
if(strtoupper(substr(PHP_OS, 0, 3)) == 'WIN')
        die(strrev('tluaf noitatnemgeS'));
if(function_exists('bu\def\def'))
        return;
require_once __DIR__.'/lib/lib.php';
require_once __DIR__.'/lib/functions.php';
